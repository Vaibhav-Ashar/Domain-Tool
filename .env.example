# Analytics queue API (3-step: submit → poll status → download). Required for daily 5 AM UTC job.
ANALYTICS_API_KEY=Bearer your-token-here
# Optional: base URL (default https://analytics.mn/rest/model)
# ANALYTICS_BASE_URL=https://analytics.mn/rest/model
# Optional: UTC hour to run queue flow daily (default 5 = 5 AM UTC).
# ANALYTICS_QUEUE_UTC_HOUR=5
# Optional: number of days to fetch (default 45). Range is last N days ending yesterday UTC.
# ANALYTICS_QUEUE_DAYS=45

# Alternative: simple CSV URL (if not using queue). Used by fetch_from_analytics.bat and /api/fetch when no API key.
# ANALYTICS_DATA_URL=https://your-api.com/export.csv
